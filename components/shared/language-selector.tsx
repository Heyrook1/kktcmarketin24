"use client"

// Language switching is not yet implemented (next-intl not installed).
// This component is intentionally removed from the header to avoid
// showing a non-functional UI element. Kept as a stub for future use.

export function LanguageSelector() {
  return null
}
